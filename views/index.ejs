<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Anonymous Reviews</title>
  <style>
    body { font-family: Arial, sans-serif; max-width: 600px; margin: 20px auto; padding: 10px; }
    form textarea { width: 100%; height: 80px; margin-bottom: 10px; }
    form button { padding: 6px 12px; background: navy; color: white; border: none; border-radius: 5px; cursor: pointer; }
    .review { border-bottom: 1px solid #ccc; padding: 10px 0; }
    .review strong { color: #444; }
    .allReviews{
        background-color: #ecf8f8;
        padding: 10px;
        margin-top: 10px;
        border-radius: 5px;
    }
    h2{
        color: #1e1729;
    }
    @media(max-width: 350px) {
        .allReviews{
            width: 250px;
        }
    }
  </style>
</head>
<body>
  <form action="/add" method="POST">
    <textarea name="content" placeholder="Write your review..."></textarea>
    <br>
    <button type="submit">Submit</button>
  </form>
  <div class="allReviews">
    <h2>All Reviews</h2>
    <div class="allReviewsInner">
        <% if (reviews.length === 0) { %>
          <p>No reviews yet.</p>
        <% } else { %>
          <% reviews.forEach(r => { %>
            <div class="review">
              <strong>Anonymous:</strong> <%= r.content %> <br>
              <small><%= new Date(r.created_at).toLocaleString() %></small>
            </div>
          <% }) %>
        <% } %>
    </div>
  </div>
</body>
</html>
