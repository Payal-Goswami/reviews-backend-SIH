<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Anonymous Reviews</title>
  <style>
    body { font-family: Arial, sans-serif; max-width: 100%; margin: 10px; padding: 25px;}
    form textarea { width: 100%; height: 200px; margin-bottom: 10px; font-size: 1.2rem; padding: 8px; }
    form button { padding: 6px 12px; background: navy; color: white; border: none; border-radius: 5px; cursor: pointer; font-size: 1.8rem;}
    .review { border-bottom: 1px solid #ccc; padding: 20px 0; }
    .review strong { color: #444; }
    
    h2{
        color: #1e1729;
    }
    @media(max-width: 350px) {
        .allReviews{
            width: 250px;
        }
    }
  </style>
</head>
<body>
  <form action="/add" method="POST">
    <textarea name="content" placeholder="Write your review..."></textarea>
    <br>
    <button type="submit">Submit</button>
  </form>
  <div class="allReviews">
    <h2>All Reviews</h2>
    <div class="allReviewsInner">
        <% if (reviews.length === 0) { %>
          <p>No reviews yet.</p>
        <% } else { %>
          <% reviews.forEach(r => { %>
            <div class="review">
              <strong>Anonymous:</strong> <%= r.content %> <br>
              <small><%= new Date(r.created_at).toLocaleString() %></small>
            </div>
          <% }) %>
        <% } %>
    </div>
  </div>
</body>
</html>
